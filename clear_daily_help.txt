
import { createClient } from '@supabase/supabase-js';
import dotenv from 'dotenv';
dotenv.config();

const supabaseUrl = process.env.VITE_SUPABASE_URL;
const supabaseKey = process.env.VITE_SUPABASE_ANON_KEY; // Using anon key might not have delete permission if RLS blocks it.
// Actually RLS allows insert but maybe not delete for anon.
// Let's check RLS in create_daily_challenges.sql...
// "Allow read access to authenticated users"
// "Allow insert access to authenticated users"
// No delete policy!

// So we might need to add a delete policy or just use a new date?
// Or I can update the service to override.

console.log("To force new generation, please run this SQL in your Supabase Editor:");
console.log("DELETE FROM daily_challenges WHERE date = CURRENT_DATE;");
